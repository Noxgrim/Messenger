package manager;

import exchange.InternalMessage;

public class MessageManager {
  
  private CommandManager ci;

  public void interpreteMessage(InternalMessage m) {
    if (m.isCommand()) {
      
    }
  }

}
